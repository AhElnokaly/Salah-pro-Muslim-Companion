/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 * Canonical Default Settings Re-export
 */

import { DEFAULT_APP_SETTINGS } from '../config/defaultSettings';

export { DEFAULT_APP_SETTINGS };
export const DEFAULT_SETTINGS = DEFAULT_APP_SETTINGS;
